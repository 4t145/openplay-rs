[package]
name = "openplay-poker"
version = "0.1.0"
edition = "2024"
[lib]


[dependencies]
rand = { workspace = true, optional = true}
serde = { workspace = true, features = ["derive"]}

[features]
default = ["rand"]


